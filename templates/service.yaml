apiVersion: v1
kind: Service
metadata:
  name: node
spec:
  type: LoadBalancer
  selector:
    app: node
    demo: helm
  ports:
  - protocol: TCP
    port: 80 #service-port
    targetPort: 80 #container-port